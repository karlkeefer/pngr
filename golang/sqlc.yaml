version: 1
packages:
  - path: "db"
    name: "db"
    engine: "postgresql"
    schema: "./sql/schema/"
    queries: "./sql/queries/"
    emit_json_tags: true
    emit_empty_slices: true
    emit_interface: true
