version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"

  # postgres is our primary data store
  psql:
    image: "postgres:10.4-alpine"

  # nginx is used for ssl termination
  nginx:
    build: nginx
    ports:
      - "443:443"